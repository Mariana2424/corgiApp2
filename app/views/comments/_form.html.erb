<%= form_for([@article, @article.comments.build]) do |a| %>
  <p>
    <%= a.label :name %>
    <%= a.text_field :name %>
  </p>
  <p>
    <%= a.label :body %>
    <%= a.text_area :body %>
  </p>
  <p>
    <%= a.submit %>
  </p>
<% end %>